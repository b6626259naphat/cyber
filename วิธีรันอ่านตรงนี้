ติดตั้ง Library ที่จำเป็น:

รัน pip install -r requirements.txt 
รัน pip install pillow

บนโฟลเดอร์ /cyber

รันโปรแกรม:

รัน python app.py 

บนโฟลเดอร์ /cyber

ถ้าพอร์ท 5000 ถูกใช้แล้ว ให้เปลี่ยนพอร์ท ที่ cyber/app.py จาก5000 เป็นพอร์ทอื่น